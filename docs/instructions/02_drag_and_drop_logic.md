- journey intents can as a rule be dropped into every other journey intent
- journey intents can also be dragged into new "ghost steps" between ANY existing step, as well as before the very first existing plan step, as well as after the last existing plan stap
- cargo intents can not be dropped into any other cargo step, since every cargo step takes only one intent
- however, as journey intents, they can be dropped into "ghost steps" between any two existing steps, as well as before the very first step, as well as after the very last step. 
- journey steps can take multiple journey intents (only one per vehicle though). They should be displayed in one row (as a flex row).
- dragging a vehicle's journey intent into a journey step that already has a journey intent for that vehicle DELETES (yes, deletes) the previous intent. Show that this will happen by coloring the existing intent's box red when the user is dragging a relevant intent box over this
- drop targets should always be visualized as a box with a dashed border
- in default state, do not show any drop targets
- as soon as the player starts dragging a cargo intent, create drop targets for all ghost steps (as very first plan item, between every item, as last item). Make them a reasonably sized box, spanning the width of the plan box, and having some reasonable mininimum height
- as soon as the player starts dragging a journey intent, create the same drop targets. FURTHERMORE, create a drop target box WITHIN each existing journey step that doesn't yet have a journey intent for this vehicle. For steps that already have a journey intent for this vehicle, make this pre-existing journey intent a drop target, outlining it dashed

- Implement this very cleanly and nicely. This is part of the core loop. It has to feel smooth, look nice, and be understandable. It won't do to have some barely recognizable UI features here. Look for best practices first.
- Make sure subsequent cargo intent invalid state is calculated correctly and cleanly, in a well-architected, non-hacky way!