- Add a "back to menu" bottom on the very bottom of the right sidebar. It must be clicked and held for 1000ms to activate.
- Above the country display in the sidebar, add a "reset plan" button. Similar, need to be helf for 1000ms.
- If a country is highlighted, show this in a badge with a yellow circle (same yellow as the marker meshes) above the country hover display in the sidebar. Add an x button in this badge, removing the highlight. When animation mode starts, automatically clear the highlight. Cleanly model the state here.
- When crates disappear because their lifetime runs out, add a message to the message queue on the top right.
- Show not only the timecost of each journey step, but also of each journey intent in it, within the intent block. Use a lighter color for the value in the blocks whose value "don't count" because it's lower than the biggest one
- Resort the main menu buttons: Daily Challenge, Play (biggest button), Seeding Settings
- Show the seed in the final game-over screen
- If according to the plan currently all crates in the game rn will be delivered, give a +500 bonus (note this in the turn budget table)
- During the animation phase, allow zooming with mousewheel (but not panning)
- On game over screen, also allow going back to main menu (make sure to model this cleanly)
- For events that have a cancel button (e.g. pin placement), catch Escape key press to abort. Style the Cancel button like the rest of the game.