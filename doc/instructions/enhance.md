Let's improve some gameplay stuff:

- Add a second value to crates: (remaining) lifetime (measured in turns). When spawning them, randomly pick whole number between 1-5. Each turn (including the one where the crate is dropped), subtract one. When at the start of a turn a crate's remaining lifetime is 0, disappear it from the map. Display the remaining life turns on the card label, with an icon
- At the beginning of each turn (including turn 0, after vehicle placement) randomly place 2-5 crates. Always make the first one a fair drop, for the rest decide randomly with 50/50 if the drop is going to be fair or just a random valid tile. Show each drop: Pan camera to tile location, show "grow" animation, small configurable delay, pan to next
- Seed each randomized decision in the game (architect this cleanly) so that a map can be replayed with exactly the same things happening (for now, crate values and locations I think is all that's really randomized (?)). Below the start game button, add a checkbox "Seeded Game", and if enabled, allow player to put in seed (web research how other games such as Balatro or Minecraft do this). Also add a "Daily Challenge" button, which starts a game seeded with something stable for the datetime.