let's simplify the game economy/gameplay.

Reduce to only one global resource: timecost. This will be currency, tracker, and thing to optimize.

No more stamps, no more hearts, no money.

We can remove the top bar completely.
Remove undo and redo functionality, and the debug download and fair tile toggle, it's not needed anymore.

Also remove the bottom bar, instead move the display which country we're hovering to the bottom of the left sidebar.

Redesign the left side-bar as follows:

As very first element (on top), show crates delivered and turn number (this is a sort of score).

Then, show a sort of revenue/cost statement.
As first element, show how much current timecost the player has (let's give the player 1000 to start with).
Then, "Travel Cost", which how much timecost the current plan would incur. 
Then, "Turn Fee", which is just something you pay per turn, say start with 100 and add 25 every turn.
Then, "Reward", which is added according to the reward of each crate that the player currently plans on delivering.
Then, the bottom line: The players timecost budget after the turn. 


Then, put the button "confirm plan", naming it "end turn".

Then, show the plan view as we're already doing.

If at the end of turn the budget is 0 or below, the game ends and we go to an endscreen, showing some stats and allowing to restart the game.